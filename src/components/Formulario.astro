---

---

<div class="form-container">
  <form class="form">
    <input type="text" name="nome" placeholder="Nome" class="input" required />
    <input type="text" name="nome" placeholder="Email" class="input" required />
    <input
      type="text"
      name="nome"
      placeholder="Whatsapp"
      class="input"
      required
    />
    <textarea name="Mensagem" placeholder="Mensagem" class="textarea"
    ></textarea>
    <button type="submit" class="submit-btn">Fale conosco</button>
  </form>
</div>

<style>
  .form-container {
    width: 50%;
    display: flex;
    justify-content: center;
  }

  .form {
    display: flex;
    flex-direction: column;
    width: 75%;
    gap: 15px;
  }

  .input,
  .textarea {
    border: 1px solid #d8d8d8;
    border-radius: 8px;
    padding: 20px;
    color: #494949;
    background-color: #ffffff;
  }

  .input {
    height: 64px;
  }

  .textarea {
    min-height: 150px;
  }

  .input::placeholder,
  .textarea::placeholder {
    color: #494949;
    font-family: var(--tipografia-fonte-primaria);
  }

  .submit-btn {
    border-radius: 8px;
    height: 60px;
    font-size: 20px;
    color: #ffffff;
    background-color: #ff6b00;
    border: none;
    font-weight: bolder;
  }
</style>
