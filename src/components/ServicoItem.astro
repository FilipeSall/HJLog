---
import type { ServiceItemProps } from "../types/components";

export type Props = ServiceItemProps;

const { title, text } = Astro.props as Props;
---

<div>
  <h3>{title}</h3>
  <p>{text}</p>
</div>
