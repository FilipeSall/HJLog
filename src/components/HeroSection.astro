---
// Hero Section Component
---

<section id="home" class="hero">
  <div class="background-image">
    <div class="overlay"></div>
    <img
      src="https://lh3.googleusercontent.com/aida-public/AB6AXuBwJtYejfXEn6bjzQH4osJwdI2Wou8RZVL5zxZgq6IsI7Vb8s_4iHPrgLrpagSMOhSMW98fVD75FouLJ0eIlobFpayD6rcFlk4CeOy_6lxB8naIkOd8Vj4JMbYgPQ7_Gz89RWVxq-AqQ96bMBLoEoWfb6YJSFWDIkrI1w2GwaYJ95zsx6FuzpQn2IUeu8gTlWVGhK4ztXcvJDiGCiYTbk5kcadVbP0ilSRkAUxPxiBYtXXJsELAdJPK8oTKYtwLD-dCnj2HGEp7ip0"
      alt="Truck on a highway at sunset representing logistics transport"
    />
  </div>

  <div class="content-wrapper">
    <div class="content">
      <div class="badge">
        <span class="pulse-dot"></span>
        <span class="badge-text">Líder em Logística</span>
      </div>

      <h1>
        Transporte ágil e seguro para mover o seu <span class="highlight">negócio.</span>
      </h1>

      <p class="description">
        Soluções completas em transporte rodoviário e logística integrada. Conectamos sua empresa a todo o Brasil com tecnologia e eficiência.
      </p>

      <div class="cta-buttons">
        <button class="primary-button">
          Solicitar Cotação
        </button>
        <button class="secondary-button">
          Rastrear Carga
        </button>
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    position: relative;
    width: 100%;
    min-height: 600px;
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
  }

  .background-image {
    position: absolute;
    inset: 0;
    z-index: 0;
  }

  .overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(to right, rgba(0, 0, 0, 0.8), rgba(0, 0, 0, 0.5), transparent);
    z-index: 10;
  }

  .background-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .content-wrapper {
    position: relative;
    z-index: 20;
    width: 100%;
    max-width: var(--container-max-width);
    padding: var(--space-20) var(--space-4);
    display: flex;
    flex-direction: column;
    justify-content: center;
  }

  @media (min-width: 768px) {
    .content-wrapper {
      padding-left: var(--container-padding-tablet);
      padding-right: var(--container-padding-tablet);
    }
  }

  @media (min-width: 1024px) {
    .content-wrapper {
      padding-left: var(--container-padding-desktop);
      padding-right: var(--container-padding-desktop);
    }
  }

  .content {
    max-width: 720px;
    display: flex;
    flex-direction: column;
    gap: var(--space-6);
    animation: fadeInUp 0.6s ease-out;
  }

  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .badge {
    display: inline-flex;
    align-items: center;
    gap: var(--space-2);
    padding: var(--space-1) var(--space-3);
    border-radius: var(--radius-full);
    background-color: var(--primary-light);
    backdrop-filter: blur(4px);
    border: 1px solid var(--primary-border);
    width: fit-content;
  }

  .pulse-dot {
    width: 0.5rem;
    height: 0.5rem;
    border-radius: var(--radius-full);
    background-color: var(--primary);
    animation: pulse 2s cubic-bezier(0.4, 0, 0.6, 1) infinite;
  }

  @keyframes pulse {
    0%, 100% {
      opacity: 1;
    }
    50% {
      opacity: 0.5;
    }
  }

  .badge-text {
    color: var(--primary);
    font-size: var(--text-xs);
    font-weight: var(--font-bold);
    text-transform: uppercase;
    letter-spacing: var(--tracking-wider);
  }

  h1 {
    color: var(--white);
    font-size: var(--text-4xl);
    font-weight: var(--font-black);
    line-height: var(--leading-tight);
    letter-spacing: var(--tracking-tight);
  }

  @media (min-width: 768px) {
    h1 {
      font-size: var(--text-5xl);
    }
  }

  @media (min-width: 1024px) {
    h1 {
      font-size: var(--text-6xl);
    }
  }

  .highlight {
    color: var(--primary);
  }

  .description {
    color: var(--text-secondary-dark);
    font-size: var(--text-base);
    font-weight: var(--font-normal);
    line-height: var(--leading-relaxed);
    max-width: 560px;
  }

  @media (min-width: 768px) {
    .description {
      font-size: var(--text-lg);
    }
  }

  .cta-buttons {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-4);
    padding-top: var(--space-4);
  }

  .primary-button {
    display: flex;
    height: 3rem;
    padding: 0 var(--space-8);
    align-items: center;
    justify-content: center;
    border-radius: var(--radius-full);
    background-color: var(--primary);
    color: var(--black);
    font-size: var(--text-base);
    font-weight: var(--font-bold);
    box-shadow: var(--shadow-primary);
    transition: all var(--transition-base);
  }

  .primary-button:hover {
    background-color: #e6e305;
    transform: scale(1.05);
  }

  .secondary-button {
    display: flex;
    height: 3rem;
    padding: 0 var(--space-8);
    align-items: center;
    justify-content: center;
    border-radius: var(--radius-full);
    background-color: rgba(255, 255, 255, 0.1);
    backdrop-filter: blur(12px);
    border: 1px solid rgba(255, 255, 255, 0.2);
    color: var(--white);
    font-size: var(--text-base);
    font-weight: var(--font-bold);
    transition: all var(--transition-base);
  }

  .secondary-button:hover {
    background-color: rgba(255, 255, 255, 0.2);
  }
</style>
